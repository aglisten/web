const PACKAGE_NAME_DEFAULT = "@aglisten/web" as const;

const INCLUDED_FUNCTIONS_DEFAULT = [
    "variables",
    "keyframes",
    "style",
] as const;

export { PACKAGE_NAME_DEFAULT, INCLUDED_FUNCTIONS_DEFAULT };
