{
    "name": "test-web",
    "private": true,
    "scripts": {
        "test": "vitest run"
    },
    "dependencies": {
        "@aglisten/web": "workspace:*",
        "vitest": "catalog:dep"
    },
    "devDependencies": {
        "vite-tsconfig-paths": "catalog:dep"
    }
}
