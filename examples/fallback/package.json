{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsdown --config tsdown.config.ts"
    },
    "dependencies": {
        "ammolite": "workspace:*",
        "react": "catalog:dep"
    },
    "devDependencies": {
        "@ammolite/rollup": "workspace:*",
        "@types/react": "catalog:dep"
    }
}
