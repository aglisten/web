{
    "private": true,
    "scripts": {
        "watch": "rolldown --watch --config rolldown.config.ts",
        "build": "rolldown --config rolldown.config.ts"
    },
    "dependencies": {
        "ammolite": "workspace:*"
    },
    "devDependencies": {
        "@ammolite/rollup": "workspace:*",
        "rolldown": "catalog:dev"
    }
}
